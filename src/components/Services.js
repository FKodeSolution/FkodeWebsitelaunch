// Re-export the main Services component and the shared `services` data
// from the richer implementation in `Services.jsx`.

export { default } from "./Services.jsx";
export { services } from "./Services.jsx";

